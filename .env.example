# URL base de la API REST. Vite la inyecta al compilar.
# Producción (Cloudflare Workers), es el default de la app:
VITE_API_URL=https://proyecto-integrador-backend.ramirezcris-cpr.workers.dev/api

# Para correr contra el backend local (npm run dev en el repo del back):
# VITE_API_URL=http://localhost:3000/api
