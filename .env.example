# URL base de la API REST. Vite la inyecta al compilar.
# Dev local (backend con npm run dev):
VITE_API_URL=http://localhost:3000/api

# Producción (cuando la API esté desplegada en Cloudflare), compilar con:
# VITE_API_URL=https://TU-API.workers.dev/api
